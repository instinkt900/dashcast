# Copy to docker/.env and fill in. This file (.env) is gitignored.
# Create the token in Home Assistant: Profile -> Security -> Long-lived access tokens.
DASHCAST_HA_TOKEN=paste-your-long-lived-access-token-here
